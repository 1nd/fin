# Google OAuth client id (Web), from a Google Cloud OAuth client with this
# app's JavaScript origins whitelisted — no redirect URIs, no client secret.
# See README.md > "Google Sign-In setup".
VITE_GOOGLE_CLIENT_ID=
